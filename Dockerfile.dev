# Dockerfile de développement avec hot reload
FROM node:18-alpine

WORKDIR /app

# Installer pnpm globalement
RUN npm install -g pnpm

# Copier tout le projet d'abord
COPY . .

# Installer les dépendances (cela va utiliser le pnpm-lock.yaml existant)
RUN pnpm install

# Exposer le port de développement
EXPOSE 55555

# Commande pour démarrer le serveur de développement avec hot reload
CMD ["pnpm", "run", "dev", "--host", "0.0.0.0"]